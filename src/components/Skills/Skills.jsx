import React from 'react'

export default function Skills() {
  return (
        <div className="skills">
          {/* {
            skills.map((skill) => (
              <p>{skill}</p>
            ))
          } */}
        </div>
  )
}
